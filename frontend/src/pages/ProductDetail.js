import React,{useEffect,useState} from 'react'; import {Link,useParams} from 'react-router-dom'; import {getProduct,money} from '../utils/api'; import './ProductDetail.css';
function ProductDetail({addToCart}){const {id}=useParams();const [p,setP]=useState(null);const [qty,setQty]=useState(1);const [error,setError]=useState('');useEffect(()=>{getProduct(id).then(setP).catch(e=>setError(e.message))},[id]);if(error)return <div className="container message">{error}<br/><Link className="button button--dark" to="/shop">Back to shop</Link></div>;if(!p)return <div className="container message">Loading timepiece…</div>;return <main className="container detail"><Link className="back" to="/shop">← Back to collection</Link><div className="detail__grid"><div className="detail__image"><img src={p.image} alt={p.name}/></div><div><p className="eyebrow">{p.brand}</p><h1>{p.name}</h1><div className="detail__price">{money(p.price)}</div><p className="detail__description">{p.description}</p><dl className="specs">{Object.entries(p.specs).map(([k,v])=><div key={k}><dt>{k.replace(/([A-Z])/g,' $1')}</dt><dd>{v}</dd></div>)}</dl><p className="stock">{p.stock} available</p><div className="purchase"><div className="quantity"><button onClick={()=>setQty(Math.max(1,qty-1))}>−</button><span>{qty}</span><button onClick={()=>setQty(Math.min(p.stock,qty+1))}>+</button></div><button className="button button--dark" onClick={()=>addToCart(p,qty)}>Add to cart</button></div></div></div></main>};export default ProductDetail;
